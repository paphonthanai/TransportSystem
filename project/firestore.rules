rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // The app currently stores its business data in browser localStorage, not
    // Firestore. Deny by default until real collections/rules are designed.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
